WL Credit V24.4.1 deployment

1. In Supabase SQL Editor, run:
   V24.4.1-CUSTOMER-BANK-LEGACY-ALIASES.sql

2. Upload/overwrite the website files in GitHub.

3. Wait for Cloudflare Pages deployment, then close old admin tabs and reopen /admin.

This patch fixes:
- record "c" has no field "account_name"
- legacy RPCs reading account_name/account_number
- newer pages saving bank_account_name/bank_account_number
